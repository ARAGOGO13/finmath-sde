# SDE Models — Numerical Simulation of Stochastic Differential Equations

Реализация численного моделирования стохастических дифференциальных уравнений из приложения C книги **Brigo & Mercurio "Interest Rate Models — Theory and Practice"**.

Проект охватывает разделы C.1–C.6: броуновское движение, схемы дискретизации Эйлера и Мильштейна, модели GBM / Vasicek / CIR, процесс Пуассона.

---

## Быстрый старт

### Зависимости

Требуется только:
- **CMake** ≥ 3.16
- **C++17** компилятор (GCC, Clang, MSVC)
- **Git** (для скачивания Matplot++ при сборке)

На macOS дополнительно нужен gnuplot для отрисовки графиков:
```bash
brew install gnuplot
```

На Ubuntu/Debian:
```bash
sudo apt install gnuplot
```

Matplot++ скачается и соберётся **автоматически** через CMake FetchContent при первой сборке — ничего устанавливать вручную не нужно.

---

### Сборка и запуск

```bash
git clone https://github.com/YOUR_USERNAME/sde_models.git
cd sde_models

mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build . --parallel

./sde_models
```

Графики сохраняются в папку `plots/` рядом с бинарником.

> **Первая сборка** занимает 3–5 минут — идёт компиляция Matplot++.  
> Повторные сборки быстрые.

---

## Структура проекта

```
sde_models/
├── CMakeLists.txt
├── src/
│   ├── main.cpp                           # Точка входа, все эксперименты
│   ├── sde_base.hpp                       # Базовый интерфейс СДУ
│   ├── monte_carlo_engine.hpp             # Схемы Эйлера и Мильштейна
│   ├── convergence_analyzer.hpp           # Анализ сходимости схем
│   ├── girsanov_experiment.hpp            # G1 — проверка теоремы Гирсанова
│   ├── hedging_experiment.hpp             # G2 — дискретный delta-hedging
│   ├── plotting.hpp                       # Визуализация
│   ├── plot_ratio.hpp                     # Графики V_t = S_t/U_t
│   ├── plot_girsanov.hpp                  # Графики G1
│   ├── plot_hedging.hpp                   # Графики G2
│   └── models/
│       ├── geometric_brownian_motion.hpp  # GBM — модель Блэка-Шоулса
│       ├── inverse_gbm.hpp                # 1/GBM
│       ├── ratio_gbm.hpp                  # V_t = S_t/U_t, коррелированные GBM
│       ├── ornstein_uhlenbeck.hpp         # Vasicek — модель ставок
│       ├── cir.hpp                        # CIR — квадратно-корневой процесс
│       ├── poisson_process.hpp            # Процесс Пуассона
│       └── black_scholes.hpp              # BS: аналитика + MC под Q и P
└── plots/                                 # Генерируется автоматически
```

---

## Что демонстрирует проект

### C.1 — Броуновское движение
Траектории непрерывны, но нигде не дифференцируемы: приращение `dW ~ sqrt(dt)`, поэтому `dW/dt ~ 1/sqrt(dt) → ∞`.

### C.3 — Схемы Эйлера и Мильштейна
PDF утверждает что Мильштейн точнее — проект это **измеряет**:

```
Схема        Порядок сходимости   Наклон log-log (замеренный)
-----------  -------------------  ---------------------------
Эйлер        O(sqrt(dt))          ~0.50
Мильштейн    O(dt)                ~1.00
```

Поправка Мильштейна: `0.5 * σ * σ'(x) * (dW² - dt)`

При `σ'(x) = 0` (Vasicek) — схемы совпадают, что подтверждается численно.

### C.4 — Три модели процентной ставки

| Модель | СДУ | X < 0? | Мильштейн ≠ Эйлер? |
|--------|-----|--------|---------------------|
| GBM | dX = μX dt + σX dW | нет (лог-норм.) | да |
| Vasicek | dX = θ(μ-X)dt + σ dW | возможно | нет |
| CIR | dX = θ(μ-X)dt + σ√X dW | нет (при усл. Феллера) | да |

**Нетривиальный эффект GBM**: из-за поправки Ито среднее и медиана расходятся:
```
E[X_t]      = X₀ · exp(μt)               — среднее
median(X_t) = X₀ · exp((μ - σ²/2)t)      — медиана (медленнее!)
```

### C.6 — Процесс Пуассона
Верификация: `E[N_T] = Var[N_T] = λT` — характеристическое свойство распределения Пуассона.

---

## Финансовые приложения

### G1 — Теорема Гирсанова (численная верификация)

Цена европейского колла вычисляется **тремя способами**:
- Аналитическая формула Блэка-Шоулза
- MC под risk-neutral мерой Q (дрейф = r)
- MC под physical-мерой P (дрейф = μ) с перевзвешиванием `dQ/dP = exp(−½λ²T − λW_T)`

Все три должны давать одно и то же число — это и есть теорема Гирсанова. Однако при больших `|μ − r|` дисперсия P-оценщика меняется экспоненциально по `λ²·T`: это численная цена смены меры и обоснование выбора risk-neutral меры в практике оценивания.

Графики: `G1_girsanov_merged.pdf` — цены трёх оценщиков с error bars + Var-ratio vs λ в лог-шкале.

### G2 — Дискретный delta-hedging

Продавец колла получает премию `BS(σ_impl)` и хеджируется дельтой, ребалансируя `N` раз. Терминальный P&L = банк + хедж − payoff.

**Три подэксперимента:**

| Эксперимент | Что показывает |
| --- | --- |
| Гистограммы для N ∈ {10, 50, 250, 1000} | При росте N распределение P&L сжимается к δ-функции в нуле |
| std(P&L) vs √Δt в log-log | Наклон ≈ +0.5 — классический результат Bertsimas-Kogan-Lo (1998) |
| mean(P&L) vs σ_real при σ_impl = 0.20 | Если σ_real > σ_impl, продавец теряет; теория: `0.5·Γ·S²·(σ²_impl − σ²_real)·T` |

Это прямая демонстрация двух реальных торговых феноменов:
1. **издержки дискретности хеджирования** — всегда ненулевой risk даже при совпадении vol'ей;
2. **volatility arbitrage** — систематический P&L при неправильной оценке σ.

Графики: `G2_hedging_merged.pdf` (3 страницы).

---

## Связь с практикой

- **GBM** → ценообразование опционов (модель Блэка–Шоулса)
- **Vasicek / CIR** → модели краткосрочной процентной ставки
- **Процесс Пуассона** → моделирование момента дефолта в intensity-based моделях кредитного риска (Cox process / процесс Кокса)
- **Схема Мильштейна** → точное ценообразование path-dependent деривативов